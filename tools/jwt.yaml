identity:
  name: jwt
  author: bowenliang123
  label:
    en_US: jwt
    zh_Hans: jwt
    pt_BR: jwt
description:
  human:
    en_US: A tool to encode and decode JSON Web Tokens (JWT).
    zh_Hans: A tool to encode and decode JSON Web Tokens (JWT).
    pt_BR: A tool to encode and decode JSON Web Tokens (JWT).
  llm: A tool to encode and decode JSON Web Tokens (JWT).
parameters:
  - name: query
    type: string
    required: true
    label:
      en_US: Query string
      zh_Hans: 查询语句
      pt_BR: Query string
    human_description:
      en_US: A tool to encode and decode JSON Web Tokens (JWT).
      zh_Hans: A tool to encode and decode JSON Web Tokens (JWT).
      pt_BR: A tool to encode and decode JSON Web Tokens (JWT).
    llm_description: A tool to encode and decode JSON Web Tokens (JWT).
    form: llm
extra:
  python:
    source: tools/jwt.py
