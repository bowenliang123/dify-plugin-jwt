identity:
  name: jwt_encoder
  author: bowenliang123
  label:
    en_US: JWT Encoder
    zh_Hans: JWT 编码
    pt_BR: JWT Encoder
description:
  human:
    en_US: Generate encoded JSON Web Tokens (JWT).
    zh_Hans: 生成 JSON Web Tokens (JWT).
    pt_BR: Generate encoded JSON Web Tokens (JWT).
  llm: A tool to encode JSON Web Tokens (JWT).
parameters:
  - name: payload
    type: string
    required: true
    label:
      en_US: payload JSON string
      zh_Hans: 载荷JSON明文
      pt_BR: payload JSON string
    human_description:
      en_US: payload JSON string
      zh_Hans: 载荷JSON字符串
      pt_BR: payload JSON string
    llm_description: payload JSON string
    form: llm
  - name: algorithm
    required: true
    type: select
    label:
      en_US: encryption algorithm
      zh_Hans: 加密算法
      pt_BR: encryption algorithm
    human_description:
      en_US: encryption algorithm
      zh_Hans: 加密算法
      pt_BR: encryption algorithm
    default: HS256
    options:
      - value: HS256
        label:
          en_US: HS256
      - value: RS256
        label:
          en_US: RS256 (RSA)
      - value: PS256
        label:
          en_US: PS256 (RSA)
      - value: EdDSA
        label:
          en_US: EdDSA (Ed25519)
      - value: ES256
        label:
          en_US: ES256 (ECDSA)
    llm_description: encryption algorithm
    form: llm
  - name: key
    type: secret-input
    required: true
    label:
      en_US: encryption key
      zh_Hans: 加密秘钥
      pt_BR: encryption key
    human_description:
      en_US: encryption key
      zh_Hans: 加密秘钥
      pt_BR: encryption key
    llm_description: secret key for encryption
    form: llm
  - name: headers
    type: string
    required: false
    label:
      en_US: Headers JSON string
      zh_Hans: Headers JSON字符串
      pt_BR: Headers JSON string
    human_description:
      en_US: Headers JSON string
      zh_Hans: Headers JSON字符串
      pt_BR: Headers JSON string
    llm_description: Headers JSON string
    form: llm
extra:
  python:
    source: tools/jwt_encoder.py
